if(other.id != creator) {
other.hp -= damage;

with(other) {
var dir = direction;
motion_add(dir + 180,other.knockback);
if(oDamage.creator != oPlayer.id) {
        target = other;
        }
    }
}

